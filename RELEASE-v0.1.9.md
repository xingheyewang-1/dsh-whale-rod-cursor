# v0.1.9 — 破防四档 · 真脱钩 · 弹窗层级修复 · 性能优先

从 v0.1.2 到 v0.1.9，一共修了 **7 个真问题**、加了 **两套玩法**、并把"鼠标性能"当成硬指标做了一轮优化。

## ✨ 新玩法：破防四档（v0.1.4 → v0.1.8）

连着猛甩，她会一档一档升级（**门槛、冷却、时长全都能在设置页自己调**）：

| 档 | 触发（默认） | 表现 |
| --- | --- | --- |
| ① 晃晕 | 单次猛甩 | 老行为：尖叫池随机，字号随力度长到"和她一样大" |
| ② 求饶 | 窗口内 2 次 | 换成求饶台词（「别、别甩了…我真的会哭的」） |
| ③ 破防 | 窗口内 4 次 | 骂人 + 哭腔（「甩甩甩，你是把鼠标当健身器材是吧！！」）· **她开始发抖** · 2.3 秒后再补一句嘟囔 |
| ④ 摆烂 | 窗口内 6 次 | **真脱钩**：整条鱼线淡出、竿尖留一截断线头、绳子**完全不再施力** —— 她往下沉，**你怎么甩都拉不动**；约 15 秒后飘回来撒娇 |

细节：

- 计数是**滚动窗口**（默认 8 秒）：中途歇口气不清零，认真甩约 **3 秒**就能断钩
- 断钩那一鞭**只说一句**罢工宣言，不会先蹦一句"还没断线"的台词
- 沉底时她的**整个身子都留在屏内**（不会只露个脑袋），并且像浮在水里那样轻轻上下晃
- 演出结束后计数清零，想再看一次得重新甩
- ④ 的冷却是"每次触发后重新计时"（默认 5 分钟一次），才显得珍贵

## 🐛 修掉的真问题

| 版本 | 问题 | 根因 → 修法 |
| --- | --- | --- |
| v0.1.9 | 静息时仍在白烧（每帧都写 DOM） | 值没变也照写 + 轮询定时器多余 → **静息快车道 + 写去重 + 轮询按需**（静息写入 858 → **5** 次/120 帧） |
| v0.1.8 | 宕机期鼠标还能轻轻"牵"她 | 物理里代入了鼠标速度 → 脱钩期间把鼠标速度当 0（实测横跳 20 次，她只动 1.2px） |
| v0.1.8 | 沉底只露三分之一脑袋 | 边界只算"头顶挂点" → 改按**整身身高**算边界 |
| v0.1.7 | 甩半天才断一次 + 断线时串出「放我下来」 | 门槛与"气泡冷却下限"没对齐 → 重新标定窗口/门槛；并让断钩那一鞭**不再多说一句** |
| v0.1.5 | ④ 永远试不出来 | 老计数要求"连续不中断"，物理上凑不到 → 改**滚动窗口** |
| v0.1.5 | 破防后抖个不停 | 档位只升不降 + 发抖条件绑档位 → 发抖改**限时**，档位会自然回归 |
| v0.1.3 | **弹窗一开，鱼竿就看不见（只能盲操）** | 官方弹窗挂 `document.body`，而浮层在槽位里深处祖先链，祖先的 `overflow/transform` 毁掉 `position:fixed` 与层级 → 浮层**搬进 body 下的顶层容器**（所有弹窗一次修好）+ 全屏兜底 |

## ⚡ 性能（v0.1.9，你的要求："这是鼠标，不能影响鼠标的性能"）

实测（自检里数出来的真实数据）：

```
静息 120 帧：    5 次 DOM 写入
鼠标狂动 120 帧：858 次 DOM 写入
→ 静息开销降低约 170 倍
```

- **静息快车道**：鼠标没动 + 她彻底静下来 + 无演出时，整帧跳过计算与 DOM 写入（动画帧照旧续着，任何输入下一帧立刻恢复；留 0.5 秒心跳兜底）
- **零常驻定时器**：轮询只在拿不到会话订阅时才启
- **没有无界计数**：破防时刻表上限 64、台词"最近说过"按池子封顶、台词池只在启动合并一次

## 🎭 台词也变多了

- 尖叫池 **3 → 13 条**（纯尖叫 / 求饶 / 哭唧唧）
- 闲置小剧场 **20 → 32 条**（新增一批**腹黑嘲讽**：「我数了下，你今天抛竿 47 次，鱼护里 0 条。数据不错，很有参考价值」）
- 平静台词 **10 → 15 条**（新增腹黑：「我不生气。我只是把你的名字记在小本本上了」）
- 台词池升级会自动**合并**进你已存的池子，你自己加过/改过的行不会丢

## 自检

```
node smoke-test.cjs   →  203 项全过（连跑多遍零偶发）
```

覆盖：模块加载协议 / 挂点链条（素材→存档→产物 sha256）/ 设置迁移 / 渲染与几何（绳末端 = 头顶，
误差 0.000px）/ 物理手感曲线 / 气泡与字号 / 平静台词 / 闲置小剧场 / 保命机制（Alt+Esc）/ 弹窗层级 /
破防四档（①→②→③→④ 全链路 + 冷却 + 降级）/ 脱钩（线淡出、自由沉、鼠标牵不动、整身可见、浮沉）/
性能（静息 vs 活动写入比、计数封顶、零常驻 interval、监听器不重复注册）。

## 安装 / 升级

```bash
# 认准 profile：本插件是 Web 端插件，profile 用 web
dsh plugin --profile web add github:xingheyewang-1/dsh-whale-rod-cursor
```

装完**重启 `dsh web`**，然后硬刷新页面。仓库里已带构建好的 `lib/client.js`，不需要任何构建步骤。

> 改过 `lib/client.js` 之后**必须重启 `dsh web`**（客户端 bundle 在启动时进内存缓存，硬刷新不够）。

## 声明

Q 版鲸鱼娘形象来自 [`yanzwzz/dsh-whale-girl-pet`](https://github.com/yanzwzz/dsh-whale-girl-pet)（MIT）；
力学常数取值基准参考 `9livewolf/dsh-think-bounce-pet`（声明 MIT）。
**逐版升级明细与踩坑记录已归档进 [NOTICE.md](NOTICE.md) 第四节「版本沿革」。**

本插件不读写用户文件、不起进程、不联网、无遥测；纯装饰层 `pointer-events:none`，不影响任何点击与选择。
